# Task Manager App

This is an HNG Stage 0 task manager app built with HTML, CSS, and JavaScript. It displays a simple task card UI with each task's title, label, tag, description, due date, time remaining, and completion status.

The app also includes a complete toggle button, along with edit and delete action buttons for each task.
